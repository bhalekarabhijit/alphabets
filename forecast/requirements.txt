# timesfm[torch] pulls torch; the workflow pre-installs the smaller CPU-only
# torch wheel first so pip reuses it instead of downloading the CUDA build.
# v3 API (TimesFM3Forecaster + covariates). Pin major to avoid another silent
# rewrite like the 2.x -> 3.x break.
timesfm>=3,<4
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
